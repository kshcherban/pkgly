apiVersion: v1
kind: Service
metadata:
  name: {{ include "pkgly-chart.fullname" . }}
  labels:
    {{- include "pkgly-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pkgly-chart.selectorLabels" . | nindent 4 }}