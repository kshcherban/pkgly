#![allow(clippy::expect_used, clippy::panic, clippy::unwrap_used)]

#[derive(Debug, Clone)]
#[allow(dead_code)]
struct DummyRepository;

#[derive(Debug)]
#[allow(dead_code)]
struct DummyError;

// NOTE: this module intentionally stays minimal for now.
